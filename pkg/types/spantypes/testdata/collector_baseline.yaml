receivers:
  otlp:
    protocols:
      grpc:
processors:
  o11yspanmapper:
    groups: []
  batch: {}
exporters:
  otlp:
    endpoint: localhost:4317
service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [o11yspanmapper, batch]
      exporters: [otlp]
